# Core
pandas>=2.0
numpy>=1.24
pyarrow>=14.0

# Data
yfinance>=0.2.40
pandas_datareader>=0.10
requests>=2.31

# ML
scikit-learn>=1.3,<1.8   # FrozenEstimator lands in 1.6; the pipeline handles both
lightgbm>=4.0
shap>=0.44
joblib>=1.3

# Sentiment
transformers>=4.35
torch>=2.0

# LLM
google-genai>=0.3

# Storage / API
psycopg2-binary>=2.9
fastapi>=0.110
uvicorn>=0.27
python-dotenv>=1.0

# Removed, because nothing imports them:
#   streamlit  (frontend is Next.js)
#   tweepy     (no Twitter ingestion exists)
#   plotly     (charts are Recharts, client-side)
#   sqlalchemy (all DB access is raw psycopg2)
#   pydantic   (transitive via fastapi)
#   google-generativeai  (superseded by google-genai; having both shadows imports)
